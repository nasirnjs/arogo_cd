apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: cowsay-
  namespace: argo
spec:
  entrypoint: cowsay-step
  templates:
  - name: cowsay-step
    container:
      image: docker/whalesay:latest
      command: ["cowsay"]
      args: ["Hello from Argo Workflows!"]