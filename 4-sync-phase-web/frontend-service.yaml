# frontend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: frontend
  annotations:
    argocd.argoproj.io/sync-wave: "15"         # Same wave
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80